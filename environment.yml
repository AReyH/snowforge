name: snowcraft
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  # Core dependencies
  - snowflake-connector-python>=3.0,<4.0
  - sqlglot>=20.0
  # Dev dependencies
  - pytest>=7.0
  - pytest-cov>=4.0
  - pytest-mock>=3.12
  - ruff>=0.3.0
  - mypy>=1.8
  - mkdocs>=1.5
  - mkdocstrings>=0.24
  - mkdocstrings-python
  - mkdocs-material>=9.0
  - build>=1.0
  - twine>=5.0
